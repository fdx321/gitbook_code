# 锁
## Lock

```java
public interface Lock {
  /**
    没有获得锁，当前线程就一直休眠，不会被调度
   **/
  void lock();
   
  void lockInterruptibly() throws InterruptedException;
  boolean tryLock();
  boolean tryLock(long time, TimeUnit unit) throws InterruptedException;
  void unlock();
  Condition newCondition();
}

```